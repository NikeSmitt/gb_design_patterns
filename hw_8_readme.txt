В данном файле отображен своеобразный код-ревью на тему антипаттернов

1. Первое и общее хотел сказать, что у меня какой-то спаггети код
при попытке анализа как запускается application и подгружаются все routes в класс.
Сам реализовал, но забыл. Надо было больше комментировать.
В main.py во время импорта from controllers.fronts import secret_front, other_front
подгружаются еще все модули с контроллерами и соответственно выполняется декоратор app('...')
Неочевидно получилось, запутанный импорт

Исправил добавлением import controllers

2. Во всех post запросах слепая вера, так как нет валидации вводимых данных.
Только со стороны bootstrap есть (email)

3. Часто применял copy-paste programming и соответственно плодил ошибки и пренебрегал принципом DRY

4. В modals.py получился "маленький комок грязи" паттерны и модели в одном месте
